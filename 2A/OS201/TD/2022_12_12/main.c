#include <stdio.h>
#include <unistd.h>


char in_buffer[100];
char out_buffer[100];

int main(void) {

    // int sscanf(const char *str, const char *format, ...);
    // int sprintf(char *str, const chart *format, ...);

    return 0;
};